# JWT Settings
SECRET_KEY=a_super_secret_key_that_should_be_changed
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database credentials
POSTGRES_USER=myadmin
POSTGRES_PASSWORD=mysecretpassword
POSTGRES_DB=pms_db

# The full database connection string used by the backend application
DATABASE_URL=postgresql://myadmin:mysecretpassword@db:5432/pms_db

# Comma-separated list of allowed CORS origins
CORS_ORIGINS=http://localhost:3000,http://localhost,http://127.0.0.1:3000,http://10.12.6.254:3000
# This is the configuration file for PRODUCTION.
# Do NOT use this for development.

# --- PostgreSQL Database Settings ---
# These are the credentials for your PostgreSQL database.
# You can change these, but the defaults are fine for a standard setup.
POSTGRES_SERVER=db
POSTGRES_USER=pms_user
POSTGRES_PASSWORD=pms_password
POSTGRES_DB=pms_db

# Full database connection URL used by the backend.
# This should not need to be changed if you are using the provided Docker setup.
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_SERVER}/${POSTGRES_DB}

# --- Backend Security Settings ---
# IMPORTANT: You MUST generate a new, strong secret key for your instance.
# A new key will be generated for you automatically when you run ./configure.sh
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200 # 30 days

# --- CORS (Cross-Origin Resource Sharing) ---
# A comma-separated list of origins that are allowed to connect to the backend API.
# You MUST change this to the domain or IP address where you will access the application.
# Example: CORS_ORIGINS=http://localhost,http://192.168.1.100,https://pms.yourdomain.com
CORS_ORIGINS=http://localhost,http://127.0.0.1,librephotos.aashish.ai.in

# --- Application Environment ---
# Set to "production" to disable debug features. Do not change this.
ENVIRONMENT=production

# --- Redis Cache Settings ---
# These settings are for the Redis cache service.
# The defaults are fine for a standard setup.
REDIS_HOST=redis
REDIS_PORT=6379

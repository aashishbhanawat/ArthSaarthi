services:
  backend:
    ports:
      - "8001:8000"
    volumes:
      - ./backend:/app

  frontend:
    ports:
      - "30100:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev

# Step 1: Clean up previous environment
docker compose -f docker-compose.yml -f docker-compose.test.yml down -v

# Step 2: Run upgrade to head
docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm test alembic upgrade head

# Step 3: Run downgrade to base
docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm test alembic downgrade base

# Step 4: Run final upgrade to head
docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm test alembic upgrade head


#Lint test
docker compose run --rm backend sh -c "pip install -r requirements-dev.in && ruff check . --fix"
docker compose run --rm frontend npx eslint .

# Backend test
docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm test
docker compose -f docker-compose.yml -f docker-compose.test.sqlite.yml run --rm test-sqlite
docker compose -f docker-compose.yml -f docker-compose.test.desktop.yml run --build --rm test-desktop

# frontend test
docker compose run --rm frontend npm test

# E2E test
docker compose -f docker-compose.yml -f docker-compose.e2e.yml down -v --remove-orphans
docker compose -f docker-compose.yml -f docker-compose.e2e.yml up --build --abort-on-container-exit
docker compose -f docker-compose.yml -f docker-compose.e2e.yml down -v --remove-orphans

docker compose -f docker-compose.yml -f docker-compose.e2e.sqlite.yml down -v --remove-orphans
docker compose -f docker-compose.yml -f docker-compose.e2e.sqlite.yml up --build --abort-on-container-exit
docker compose -f docker-compose.yml -f docker-compose.e2e.sqlite.yml down -v --remove-orphans


# Install security tools and run checks for security and vulnerability issues
docker compose run --rm backend sh -c "pip install bandit safety && bandit -r app -x app/tests && safety check"
docker compose run --rm frontend npm audit
docker compose run --rm frontend npm audit --audit-level=high


docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm --build test pytest app/tests/api/v1/test_ppf_holdings.py
docker compose -f docker-compose.yml -f docker-compose.test.sqlite.yml run --rm test-sqlite pytest app/tests/api/v1/test_bonds.py

docker compose -f docker-compose.yml -f docker-compose.e2e.yml down -v --remove-orphans
docker compose -f docker-compose.yml -f docker-compose.e2e.yml run --build --rm test-e2e npx playwright test tests/ppf-modal-verification.spec.ts
docker compose -f docker-compose.yml -f docker-compose.e2e.yml down -v --remove-orphans

docker compose -f docker-compose.yml -f docker-compose.e2e.sqlite.yml run --build --rm e2e-tests npx playwright test tests/analytics.spec.ts


docker compose run --rm backend python run_cli.py test-bhavcopy

docker-compose run --rm backend python -m app.scripts.seed_transactions

docker compose run --rm backend python run_cli.py db init-db
docker compose run --rm backend python run_cli.py db clear-cache

docker compose run --rm backend python run_cli.py db clear-assets >> log.txt
docker compose run --rm backend python run_cli.py db seed-assets --debug >> log.txt
docker compose run --rm backend python run_cli.py db dump-table assets >> log.txt
docker compose run --rm backend python run_cli.py db dump-table bonds >> log.txt


sudo systemctl stop postgresql
cd /media/data/App/AntiGravity/cloud-workstations-custom-image-examples/examples/images/gnome/noVnc/data/.gemini/antigravity/ArthSaarthi
docker compose run --rm backend python run_cli.py db clear-cache

docker compose up --build --no-deps --force-recreate

docker compose -f docker-compose.desktop.debug.yml down -v
docker compose -f docker-compose.desktop.debug.yml up --build --no-deps --force-recreate

# Run desktop mode (SQLite + disk cache) with DEBUG logging:
docker compose -f docker-compose.desktop.yml up --build

# Or with LOG_LEVEL=DEBUG:
LOG_LEVEL=DEBUG docker compose -f docker-compose.desktop.yml up --build

ssh localhost "command"
sudo systemctl stop  postgresql
cd /media/data/App/AntiGravity/cloud-workstations-custom-image-examples/examples/images/gnome/noVnc/data/.gemini/antigravity/ArthSaarthi
docker compose run --rm backend sh -c "pip install -r requirements-dev.in && ruff check . --fix"

merger and demerger

// In browser console:
localStorage.setItem('debug', 'true')
location.reload()

1. Create an issue on git if missing
2. Create new branch
3. Update/Create FR for feature development or major enhancement, for FR numbering refer @requirements.md
4. Enable enough logs to debug issue during development (or protected logs that can enable using centralized flags in .env file)
5. Add all relevant new tests (backend, frontend, e2e) to catch regression
6. Run all tests using docker over ssh
7. Wait for manual e2e results before raising PR
8. Update all relevant document task_prompt/COMMIT_TEMPLATE.md before raising PR
9. Update requirements.md and FR to mark completed


Test data
1. Stocks - 
    1. Merger - DHANI -> INDIABULLS
    2. Split - Tata Steel
    3. Bonus - Bharat Rasyan
    4. Dmerger - ITC -> ITC Hotels
    5. Dividend - 
    6. Sell (partially) -
2. ESPP
3. RSU
4. MF
    1. Sell (partially)
    2. Reinvestment


